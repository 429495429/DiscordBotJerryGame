module.exports = {
    name: '捅pg',
    description: 'repley to HeyReplies with the true goal of 捅pg!!',
    // devOnly: Boolean,
    // testOnly: true,
    
    callback: (client, interaction) => {
        interaction.reply('一起捅0鱼pg!');
    },
};